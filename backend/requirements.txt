# Backend
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
pydantic>=2.9.0,<3.0
pydantic-settings>=2.6.0,<3.0
PyYAML>=6.0,<7.0

# Azure OpenAI
openai>=1.50.0,<2.0

# Azure Identity (Managed Identity + DefaultAzureCredential)
azure-identity>=1.19.0,<2.0

# Azure Cosmos DB
azure-cosmos>=4.9.0,<5.0

# Azure Data Lake Storage
azure-storage-file-datalake>=12.17.0,<13.0

# Azure AI Content Safety (Phase 2+)
# azure-ai-contentsafety>=1.0.0,<2.0

# Grounding with Bing Search via Azure AI Foundry (optional — lazily imported
# by backend/tools/web_search.py; only needed when a real Bing connection is
# configured. Dev uses the MockWebSearchProvider, so these stay commented.)
# azure-ai-projects>=1.0.0b7,<2.0
# azure-ai-agents>=1.0.0,<2.0

# Azure Monitor / OpenTelemetry (Phase 2+)
# azure-monitor-opentelemetry>=1.6.0,<2.0

# Redis (optional semantic cache)
# redis>=5.0.0,<6.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24.0
httpx>=0.27.0  # for FastAPI TestClient
